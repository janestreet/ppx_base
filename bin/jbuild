(executables
 ((names (main))
  (libraries (ppx_base ppx_driver))
  (link_flags (-linkall))
  (preprocess no_preprocessing)))

(provides (ppx-base (file main.exe)))

(install
 ((section bin)
  (files ((main.exe as ppx-base)))))

(jbuild_version jane_street)
